/bin/
# `go build ./cmd/...` drops a binary named after the package in the
# working directory — never something to commit.
/warden
*.log
.DS_Store

# generated per-competition secrets (scripts/generate-keys.sh) — never commit
/secrets/
